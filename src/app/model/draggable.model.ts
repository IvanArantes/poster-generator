import { ElementRef } from '@angular/core';

export class Draggable {
    File: string;
    width: string;
    height: string;
    innerText: string;
    backgroundType: string;
    zIndex: number;
    parent: ElementRef;

}